[pytest]
testpaths = tests
pythonpath = .
deps = -rrequirements-dev.txt
commands = pytest
addopts = 
  --maxfail=1
  --disable-warnings
  --cov=app
  --cov-report=term-missing
markers =
  integration: mark test as integration
